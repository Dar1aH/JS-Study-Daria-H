// Math - an intrinsic object that provides basic mathematics functionality 
// and constants. 

let x = 3.14;
let y = 5;
let z = 9;
let maximum;
let minimun;

//x = Math.round(x); - this will round the number
//x = Math.floor(x); - the floor function will always round the number down
//x = Math.ceil(x); - the ceiling function will round the number up
//x = Math.pow(x, 2); - x to the power of 2
// x = Math.sqrt(x); - the square root of x 
//x = Math.abs(); 

//maximum = Math.max(x, y, z);
//minimun = Math.min(x, y, z);
x = Math.PI;

console.log(x);